This project has been created as part of the 42 curriculum.

This project is about making a replica of bash, in in this project the built in commands of bash like (echo, cd, pwd ...) is also replicated usage of pipe and operators >, <, >> and << is also posible.

use make to generate an executable, the executable takes no argument.
